import 'package:flutter/material.dart';

class AppColors {
  // Colors
  static const primaryBlue = Color(0xFF041B4D);

  // Gradients
  static const LinearGradient primaryGradient =
      LinearGradient(colors: [Color(0xFF041B4D), Color(0xFF082971)]);
}
